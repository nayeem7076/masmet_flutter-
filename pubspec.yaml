name: messmate_app_full
description: MessMate full working mess manager app.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  shared_preferences: ^2.2.3
  intl: ^0.19.0
  http: ^1.2.2

flutter:
  uses-material-design: true
  assets:
    - assets/images/
